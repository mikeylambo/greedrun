# Godot 4 engine-generated files. This project is tracked as source only;
# the editor regenerates all of these on first open.
.godot/

# Import metadata and resource UIDs (regenerated by the editor on import).
*.import
*.uid

# Export artifacts
build/
export/
*.tmp
