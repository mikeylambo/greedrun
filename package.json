{
  "name": "greedrun",
  "private": true,
  "description": "Dev tooling for the GreedRun web build (region tests).",
  "scripts": {
    "test:web": "node tests/web_region_test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
